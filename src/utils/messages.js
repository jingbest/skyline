import { gettext } from 'ttag';

export const nameMessage = gettext(
    'Invalid: The name should start with upper letter, lower letter or chinese,' +
    ' and be a string of 1 to 128, ' +
    'characters can only contain "0-9, a-z, A-Z, "-\'_()[].:^<>".');
